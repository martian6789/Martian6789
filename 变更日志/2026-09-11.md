# 2026-09-11

## 抖音视频对比任务（桌面 MHTML 快照 vs 抖音视频文件夹）
- 源：桌面《哲学之乡》陈一言的抖音.mhtml（156 个视频卡片）；本地：桌面\抖音视频（155 个 mp4）
- 解析方法：Python quopri 解码 MHTML → 正则配对 `<a href=/video/ID>` + `img alt=标题`；文件名截断故用归一化+最长公共子串+包含关系模糊匹配（阈值0.6）
- 结果：151 个标题匹配一致；网页缺 5 个本地缺失（7665936241304656379 / 7650636256003016049 / 7649332145118321329 / 7641816664347559537 / 7684101098046573887 无标题）；3 组重复（高低清两版，用户选择全部保留）；1 个快照外文件 7644351707111707761.mp4 建议保留
- 抖音反爬：yt-dlp 要 fresh cookies；iesdouyin share 接口返回 JS-glue 页；agent-browser 真实浏览器（含 headed、无代理直连）仍黑屏。结论：无登录态无法自动下载，已把链接清单交用户自行补齐
- 工具备忘：agent-browser 装在 C:\Users\Administrator\.workbuddy\binaries\node\workspace（非全局），二进制 bin\agent-browser-win32-x64.exe，需要 PATH 前置托管 node；管道到 head 会致 SIGTERM，须重定向文件读输出；环境代理 127.0.0.1:8118
- 交付：output\抖音视频对比报告.html

## 补齐下载（同日下午，成功）
- 方案：agent-browser 打开移动端分享页 iesdouyin.com/share/video/<id>（会自动跳转且能正常渲染；直接开 douyin.com/video/<id> 是空白页）→ HAR 录制 → 取 douyinvod 媒体流 URL → curl 带 Referer 下载 → ffmpeg 校验/合并
- 关键：URL 带 `&__vid=<id>` 才是目标视频；不带的高码率流多为同视频 ABR 升档（PSNR 45dB 证实），需比对时长确认；页面还会预加载推荐视频流（曾误下）
- 已下载 4 个：宇宙起源 _庄子.mp4(9.2MB)、在晋代司马懿家族管理…竹林七贤。.mp4(5.1MB)、读书，庄子。.mp4(12.8MB)、读书，庄子。第八篇。骈拇。.mp4(20MB)，均含音轨
- 更正：7684101098046573887 是网页页脚「热门」推荐链接，非该账号视频 → 网页实际 155 个，非 156；真实缺失 4 个已全部补齐
- ffmpeg 来源：npm install ffmpeg-static --registry=https://registry.npmmirror.com（82MB，在托管 workspace）
- 已沉淀技能：~/.workbuddy/skills/douyin-video-download/SKILL.md

## 内容级复核（傍晚，推翻此前结论）
- 用户追问"能确认一致同步了吗"→ 做了内容级校验，发现此前基于标题匹配的结论不可靠
- 数量/标题层面：网页 155 个视频，本地 159 个文件，155/155 全覆盖 ✓
- 内容层面抽查 13 个：**仅 3 个与网页视频一致，10 个不符**（差值 4.44s~191s，有正有负）
  - 深度验证 3 个（PSNR 11.6 / 14.1 / 16.4 dB，同视频不同清晰度应为 ~45dB）→ 确认是完全不同的视频
  - 典型：7652913188400497073 网页 229.93s，本地"人生就像打游戏…"368.70s
- 更正：此前判定的"3 组重复"全是误判（7:59 vs 0:20、22:28 vs 2:42、1:47 vs 2:23），实为内容错位文件
- 成因：抖音页预加载推荐位流，用户原下载工具未用 `__vid` 锁定 → 名对内容错
- 本次新下载的 4 个均通过时长校验（差值 0.00~0.04s），确认正确
- 教训：**判定"重复"必须先比时长；"是否同步"必须做内容级校验，不能只比标题**
- 交付：output\抖音视频对比报告.html（已重写为内容级校验版）

## 全量修复（晚间，用户授权"增删改自己做主"）
目标：让本地与网页 155 个视频内容完全一致。全流程约 2.5 小时。
- 流程：本地指纹(ffmpeg 并发提时长) → 网页全量时长采集(155个/30分钟,断点续传) → 按时长贪心一对一匹配(容差0.5s) → 歧义项 PSNR 消解 → 两阶段重命名 → 补下载 → 全量复核
- 结果：**155/155 完全一致**（文件名+内容双重校验），本地无多余文件，3.44GB
  - 22 个内容已正确（18 个规范命名）、102 个重命名、31 个重新下载、35 个移入备份
  - 账目：159 − 35 + 31 = 155 ✓
- 关键决策：本地其实存着大部分正确内容只是贴错名字 → 以"重分配文件名"为主，仅补下 31 个
- 歧义处理：38 个 vid 有多个时长候选 → 8 个候选内容相同可任选，30 个内容不同必须重下（其中 9 个属 locked 保留原文件）
- 最终深度抽检：PSNR 45.38dB 确认一致（修复前问题样本为 11.6/14.1/16.4dB）
- 踩坑记录：
  1. Windows 下 Python 文本模式写文件把 \n 转 \r\n → ID 列表读入带 \r → 拼进 JSON 使一行裂成两行，整个 jsonl 解析失败（一次丢 21 条数据）。必须 `VID="${VID//[$'\r\n']/}"` + `open(newline='\n')`
  2. 规整化 jsonl 不能原地 `open(p,'w')`（异常即截断），用 tempfile+os.replace
  3. bash `[ -f "$中文路径" ]` 判定失效，改用 Python os.path.exists
  4. PSNR 输出：`stats_file=-` 写到 stdout(`psnr_avg:`)，summary 在 stderr(`average:`)，值可能是 inf（字节相同），正则要覆盖
- 备份：桌面\抖音视频_备份_20260911（35 个 / 0.63GB），未删除任何文件
- 交付：output\抖音视频同步修复报告.html
- 技能已更新：~/.workbuddy/skills/douyin-video-download/SKILL.md 增补"批量修正名对内容错的完整方案"

## 抖音批量下载器 GUI（夜，进行中）
用户需求：EXE 单文件、美观界面、内置登录浏览器+保存 Cookie、一键批量下载主播全部视频、默认最高画质、保存目录选择栏。
- 项目：douyin-downloader/（main.py + app/），技术栈 PySide6 + QtWebEngine + requests + ffmpeg，PyInstaller 打包
- 环境：venv C:\Users\Administrator\.workbuddy\binaries\python\envs\douyin-dl（PySide6 6.11.2 + Addons + requests + PyInstaller）
  - 注意：PySide6-Essentials **不含** WebEngine 二进制，必须再装 PySide6-Addons
  - 镜像速度：腾讯源 26MB/s（aliyun 很慢，曾 19 分钟未完成）；装大包会被沙箱守卫中断，需 dangerouslyDisableSandbox
- 已验证通过：
  1. GUI 冒烟启动 OK（tools/smoke_test.py）
  2. 解析+下载真实视频 OK：7650636256003016049 → 37.33s/1024x576 与网页一致（tools/functional_test.py）
  3. 主页抓取能命中作品容器 ul[data-e2e="scroll-list"]（排除底部推荐位）
- 已修复的坑：**QRunnable 局部变量被 GC → sigDone 永不触发**（文件下好了但 UI 无反应）。DownloadManager 必须持有 task 引用列表，完成后释放
- 待办（明天继续）：
  1. 滚动加载只拿到 21/155 个 —— 需诊断（设 DY_DEBUG=1 可打印 h/y/el）；可能需登录后、或滚动容器/IntersectionObserver 问题
  2. 打包：先 onedir 验证 QtWebEngine 资源（qtwebengine_resources.pak / icudtl.dat / QtWebEngineProcess.exe），再 onefile；--add-binary ffmpeg.exe
  3. ffmpeg 已放 app/bin/ffmpeg.exe（82MB，来自 ffmpeg-static）
  4. 图标已生成（tools/make_icon.py 纯标准库写 ICO）


## 抖音批量下载器 EXE（夜-凌晨，完成打包）
- 打包：tools/build.py PyInstaller --onefile，--add-binary app/bin/ffmpeg.exe --add-data app
- 排除列表：先全排除 → 启动报 QtWebEngineProcess 找不到 → 保留 QtPrintSupport / QtNetworkAuth / QtWebSockets / QtWebChannel / QtQml / QtQuick 后通过
- 结果：dist/抖音批量下载器.exe 283MB，启动后窗口可见
- 教训：PySide6 排除时不能拍脑袋先全砍再补，要查实际依赖
- 反爬坑（已埋雷）：试过用 Chrome 调试端口（--user-data-dir 复制到临时目录）读 v20 加密 Cookie，结果把真实 Chrome 的 Cookie DB 清成 0 条（123→0）。已删除 chrome_cdp.py 改回需用户扫码/手动登录内置浏览器

## AI 剪辑软件（已暂停）
- 项目：ai-video-editor/，独立 venv：C:/Users/Administrator/.workbuddy/binaries/python/envs/ai-editor
- 已完成：视觉特征/音频特征提取、节拍检测、高光选段引擎、AI 原创程序作曲（4拍型×4情绪）、字幕引擎、渲染管线骨架
- 实测进度：分析+选高光+配乐生成全跑通（74s 分析，35 个节拍点，BPM 128）；渲染失败——ffmpeg 拒绝 filter_complex，疑因字幕滤镜的 filename 参数对中文+特殊字符路径敏感，需改用绝对路径或将 ASS 拷到临时目录
- 待办：1) 修复字幕烧录 2) GUI（PySide6） 3) PyInstaller 打包
