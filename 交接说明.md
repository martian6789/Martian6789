# 抖音批量下载器 · 源码交接说明

当前版本：**v1.3.2**（2026-09-12）
程序本体：`dist/抖音批量下载器.exe`（打包产物，不在本交接包内）

---

## 一、这是什么

一个 Windows 桌面程序（Python + PySide6/QtWebEngine），用于批量归档抖音账号的视频：
抓取主播主页全部作品 → 解析直链 → 断点续传下载为 MP4（最高画质）。

---

## 二、目录结构

```
源码/
├── main.py              程序入口
├── app.ico              程序图标
├── app/                 全部业务代码
│   ├── config.py        配置/路径/默认值（APP_NAME、并发、保存目录）
│   ├── main_window.py   主窗口与侧边栏
│   ├── ui_download.py   批量下载页（列表、全选、下载控制）
│   ├── ui_browser.py    内置浏览器页（登录）
│   ├── ui_settings.py   设置页 + 使用帮助
│   ├── webengine.py     Chromium Profile / Cookie 持久化（登录态）
│   ├── scraper.py       主页滚动采集（含数量校准）
│   ├── resolver.py      播放页解析直链（视图池 + 验证码检测）
│   ├── downloader.py    断点续传下载（HTTP Range + .done 标记）
│   ├── runlog.py        运行日志（写入 AppData/.../logs）
│   ├── style.py         界面样式
│   └── bin/ffmpeg.exe   分离音视频合并用的内置 ffmpeg（打包必需）
└── tools/               构建与调试脚本
    ├── build.py         ★一键打包
    ├── smoke_ui.py      离屏冒烟：实例化全部页面（打包前必跑）
    ├── test_checkall.py 表头全选联动测试
    └── probe_*.py       各类联调探针脚本
变更日志/                 每个版本的改动原因（重要，接手前先读）
参考文档/                 采集与下载的工作流经验（踩坑清单）
```

---

## 三、怎么跑起来 / 怎么打包

```bash
pip install PySide6==6.11.2 PyInstaller==6.22.2     # 依赖

python main.py                                       # 直接运行（调试）
python tools/smoke_ui.py                             # 打包前冒烟（必做）
python tools/build.py                                # 打包 → dist/抖音批量下载器.exe
```

> 本机可用的 Python 环境：
> `C:\Users\Administrator\.workbuddy\binaries\python\envs\douyin-dl\Scripts\python.exe`

---

## 四、改代码前必须知道的「三不原则」

这三条都是踩过坑才定下来的，破坏任意一条都会让程序出现**难以定位**的故障：

1. **不要改 `app/config.py` 里的 `APP_NAME`**
   它同时是数据目录名和注册表键名，改了用户已保存的登录态和设置全部丢失。
   要改显示名称请改 `APP_TITLE`。

2. **解析视图必须复用，绝不能「用完销毁再新建」**（`app/resolver.py` 的视图池）
   Chromium 渲染进程有上限，销毁重建后新建的视图再也拿不到视频流，
   表现就是「下到第 10 个以后整批失败」。

3. **打包必须同时带 `--icon` 和 `--add-binary app/app.ico`**
   只给 `--icon` 时资源管理器图标正常、但任务栏图标是白的；ffmpeg 也必须一起打包，
   否则遇到分离音视频流（dash）时无法合并。

另：`app/ui_settings.py` 里的 `HELP` 是 `%` 格式化的 HTML 字符串，
**改动后占位符个数必须与格式化参数一致**，否则程序启动即崩溃（v1.3.2 出过此事故）。

---

## 五、版本历史与改动原因

| 版本 | 主要改动 | 原因 |
|---|---|---|
| v1.2.0 | 暂停/断点续传、屏蔽 bitbrowser 弹窗、时长列、默认并发 5、勾选框改为整行点击 | 用户反馈「10 个以后整批失败」（视图池根因）、「勾选要点很多次」（Qt 双重切换） |
| v1.2.1 | 采集数量校准（读网页作品总数）、修复截断、截屏黑条改窄 | 630 个只采到 165 个（has_more 误信、作者判定错位、滚动停滞） |
| v1.2.2 | 登录徽标改为「页面 DOM 优先 + 4 秒轮询」 | 已登录却显示未登录（Qt cookie 信号不可靠 + `_forgotten` 墓碑阻塞） |
| v1.3.0 | 取消采集后自动下载、补图标、改名、默认保存桌面、验证码弹窗、失败自动重试 | 用户 7 项需求；自带浏览器无 H.264/AAC 编译支持故无法播放（不影响下载） |
| v1.3.1 | 验证码检测加入 `document.title` 判定；默认并发降为 1 | 「验证码中间页」正文为空，只查 body 永远检测不到，弹窗从不触发 → 整批失败 |
| v1.3.2 | 表头「全选」、主页「去验证」按钮、使用帮助精简 | 用户 3 项体验改进；并修复帮助文本格式化占位符不匹配导致的启动崩溃 |

更详细的逐次改动记录请看 **变更日志/** 下的日期文件。

---

## 六、换 WorkBuddy 账号后如何继续

- 工程文件在本机磁盘，不随账号迁移；新账号把工作目录指向本文件夹即可。
- 新账号没有历史对话，但**变更日志/** 里保留了每版改动原因，读一遍即可接手。
- 用户的抖音登录态与软件设置存在
  `%LOCALAPPDATA%\DouyinDownloader\` 与注册表，**与 WorkBuddy 账号无关，不受影响**。
